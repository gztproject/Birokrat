{% extends 'dashboard/layout.html.twig' %}

{% block body_id 'travelExpense_index' %}
{% block body_controllers %} expense-bundle{% endblock %}
{% block body_attr %}data-expense-bundle-url-value="/dashboard/travelExpense/bookInBundle/withFilter"{% endblock %}

{% block toolbar %}
    {{ include('components/_page_header.html.twig', {
        title: 'title.travelExpenses'|trans,
        count: pagination.getTotalItemCount,
        action_url: path('travelExpense_new'),
        action_label: 'action.create_travelExpense'|trans,
        extra_actions: '
            <button type="button" id="bookCheckedTEs" class="btn btn-sm btn-info">' ~ ('action.bookSelectedTravelExpenses'|trans) ~ '</button>
            <button type="button" id="bookVisibleTEs" class="btn btn-sm btn-info" data-action="expense-bundle#bookVisible">' ~ ('action.bookVisibleTravelExpenses'|trans) ~ '</button>
        '
    }) }}
{% endblock %}

{% block filters %}
    {{ include('dashboard/travelExpense/_filter.html.twig') }}
{% endblock %}

{% block main %}
    <div class="bs-component">
    {{ include('dashboard/travelExpense/_table.html.twig', {
            travelExpenses: pagination,
            ShowCBs: true,
            infinite: true,
            page: pagination.currentPageNumber,
            last_page: last_page,
        }, with_context = false) }}
    </div>
{% endblock %}
