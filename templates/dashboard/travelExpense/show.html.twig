{% extends 'dashboard/layout.html.twig' %}

{% block body_id 'travelExpense_show' %}

{% block main %}
    {{ include('components/_page_header.html.twig', { title: travelExpense.TravelDescription }) }}
    {% endblock %}

{% block sidebar %}
        {{ parent() }}
        <a href="{{ path('travelExpense_pdf', {'id': travelExpense.id}) }}" target="_blank" class="btn btn-sm w-100 btn-success">
            <i class="fa fa-file-pdf" aria-hidden="true"></i> {{ 'action.pdf'|trans }}
        </a>
{% endblock %}