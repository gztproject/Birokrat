{% extends 'dashboard/layout.html.twig' %}

{% block body_id 'konto_index' %}

{% block main %}
    <h1>{{ 'title.konto'|trans }}</h1>
    <div class="bs-component">
    {{ include('dashboard/codesheets/konto/_table.html.twig', {
                kontos: kontos                
            }, with_context = false) }}        
    </div>
{% endblock %}

{% block sidebar %}
    
    {{ parent() }}
    <div id="filter" class="row">
        {{ include('dashboard/codesheets/konto/_filter.html.twig', {form: form}) }}
    </div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    {{ encore_entry_script_tags('js/codesheets') }}
{% endblock %}
